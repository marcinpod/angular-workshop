import { PortalAttachToDirective } from './portal-attach-to.directive';

describe('PortalAttachToDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalAttachToDirective();
    expect(directive).toBeTruthy();
  });
});
