import { PortalTargetDirective } from './portal-target.directive';

describe('PortalTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalTargetDirective();
    expect(directive).toBeTruthy();
  });
});
